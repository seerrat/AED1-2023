#include <stdio.h>
 
int main(){
    int k, v[10];
    for (int i =0; i <= 9; i++){
       scanf ("%i", &v[i]);
    }
    for (k = 0; k <= 9; k++){
        if (v[k] <= 0){
            v[k] = 1;
        }
    printf ("X[%d] = %d\n", k, v[k]);
    }
    return 0;
}
